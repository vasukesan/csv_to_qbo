syntax
make sure that that the "qbo_template.qbo" is in the same directory
(omitted from github commit)

python csv_qbo.py <CSV file>


quick script for converting bank supplied CSV file to Quickbooks .qbo format. 

originally created May 2017

